# Notifications Feature

Owns notification display, user scoping, read/delete list updates, and notification creation helpers.

Root `src/pages/Notifications.tsx` is a compatibility shim. The real page lives at `src/features/notifications/pages/Notifications.tsx`.
