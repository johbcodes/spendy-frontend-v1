# System Setup Feature

Owns category and client setup screens plus feature-local API wrappers, rules, mappers, schemas, and hooks.

Root `src/pages/SystemSetup.tsx` is a compatibility shim. The real page lives at `src/features/system-setup/pages/SystemSetup.tsx`.
